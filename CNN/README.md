- CNNs are good for scalability.

# CNN Installation / Implementation
- pip install torch torchvision matplotlib scikit-learn
- Use MNIST dataset:
    - 28x28 grayscale images
    - 10 classes (0-9)
    - 60,000 training
    - 10,000 test
- Loaded via dataLoader.py
- Build the CNN model and train

# Inference
- pip install pillow

# SVM Installation / Implementation
- pip install scikit-learn





